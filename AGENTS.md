# 昆明积水地图 · 协作者须知

本仓库是「昆明积水地图」（kunming-flood-map）数据集与前端工程。核心产物在 `kunming-flood-map/html/`（模块化 html + css/js/data 分离）；`昆明积水地图-0818.html` 是旧的单文件入口，仅为跳转。

## Agent skills

### Issue tracker

工作项存在该仓库的 GitHub Issues，用 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

默认五类 triage 标签：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录一个 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
