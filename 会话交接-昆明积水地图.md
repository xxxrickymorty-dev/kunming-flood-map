# 会话交接 · 昆明积水地图（淹了么）

> 供新会话继续开发用。整理自既有对话（坐标校正、分区、搜索、发布、模块化重构等）。

## 产品与线上

| 项 | 值 |
|----|-----|
| 本机根目录 | `C:\Users\damn\Desktop\淹了么` |
| 唯一事实源 | `kunming-flood-map/html/`（index.html + css/app.css + js/data.js + js/app.js） |
| 数据改动 | **只改 `html/js/data.js`**；hist 中带 `ref` 的点坐标继承事件点，勿两处维护 |
| 根目录旧文件 | `昆明积水地图-0818.html` 已退化为跳转页，不再是源 |
| 线上地址 | http://43.180.135.43:8088/ |
| 容器 | Docker nginx `kunming-flood-map-nginx-1`，宿主机 **8088→80** |
| 勿动 | `ai_learning` 占用的 **:80**（`/admin/`） |
| 发布技能 | `.cursor/skills/kunming-flood-deploy/SKILL.md` |
| 发布脚本 | `kunming-flood-map/scripts/publish.ps1`（直接打包 html/ + deploy/，无同步步骤） |
| SSH | `ubuntu@43.180.135.43`，密钥 `%USERPROFILE%\.ssh\yanleme-4h8g.pem` |
| GitHub | https://github.com/xxxrickymorty-dev/kunming-flood-map （main） |

发布命令（仓库根目录）：

```powershell
powershell -ExecutionPolicy Bypass -File ".\kunming-flood-map\scripts\publish.ps1"
```

上线后提醒用户强刷缓存。

## 架构要点

- 静态站，无后端；数据手录进 `js/data.js` 的 `events` / `hist`（非实时爬取）。
- 底图：高德瓦片 **GCJ-02**。点位存的就是 GCJ（`POINT_CRS = "gcj"`）；GPS 用 `"wgs"` 转换。
- **取坐标铁律**：OSM/Nominatim = WGS84，入库前必须过 `wgs84ToGcj02`；百度 = BD-09 且游客精度只到 2 位小数（≈1km），只能当区域参考，不能直接抄。
- 顶栏：全部 / 场次 / 常年易淹 / 区域分析 / 分区分图。视图写入 URL hash（`#v=0818`、`#v=d-西山`），可分享。
- 分区分图：浅蓝行政边界来自 `districts.geojson`（DataV 县级界转 GCJ，边界近似）；**经开**为功能区示意（虚线）。
- **「机场向」已并入官渡**。
- 搜索：库内匹配 + Nominatim（已限速 ≤1 次/秒、120ms 防抖）；与积水圈做距离重叠判定（事件圈 110m+80m，常年圈 r+120m）。
- nginx：`script-src 'self'`（无 unsafe-inline）；html/css/js `no-cache` 协商缓存；vendor/图片 30 天。
- file:// 直开可用（相对路径 + data.js 全局变量），仅 districts.geojson 与 Nominatim 需网络。

## 坐标校正记录（2026-08-19 大核对）

用户反馈 27/33 点位跑偏、24 号点（十一家具城）位置不对。结论：**不是高德底图 bug**，是历史存错坐标（部分疑似把 WGS/BD 当 GCJ 用）。已按 OSM(WGS84)→GCJ-02 转换 + 高德底图目视核对修正：

| 点 | 旧 (GCJ) | 新 (GCJ) | 依据 |
|----|----------|----------|------|
| J10 华昌路×采莲路 | 25.0365,102.6815 | **25.0166,102.7049** | OSM 华昌路/采莲路（永昌街道）交叉 |
| H27 永昌小区 | 25.0374,102.6796 | **25.0205,102.7034** | 环城南路南、华昌路—云兴路、云纺对面 |
| H17 永昌片区 | 25.0374,102.6796 | **25.0200,102.7029** | 同上，片区中心 |
| J1/H12 十一家具城 | 24.9975,102.6988 | **24.9875,102.6899** | 用户指认奥宸财富广场=前卫西路×广福路（省公安厅旁，西山区前卫街道） |
| H15 豆腐营 | 25.0475,102.717（盘龙） | **25.0195,102.7079（西山）** | 豆腐营在环城南路南/海埂路，非盘龙江 |
| H22 金泰国际 | 25.0705,102.6695 | **25.0634,102.6734** | OSM 楼盘位置 |
| H23 戛纳小镇 | 25.062,102.678 | **25.0624,102.6640** | OSM 楼盘位置 |
| H24 海源北路 | 25.076,102.671 | **25.0749,102.6543** | OSM 海源北路路形 |
| H28 龙泉路红云 | 25.0785,102.7085 | **25.0944,102.7199** | 龙泉路昆八中段（常年淹水改造记录） |
| A1 金桂街 | 24.892,102.821 | **24.9015,102.7915** | OSM 金桂街东段 |
| A2 兴呈路小古城 | 24.905,102.805 | **24.8903,102.7957** | OSM 兴呈路古城段 |
| E6 石虎关立交 | 25.02311,102.74229 | **25.0202,102.7438** | 旧值与 OSM(WGS) 完全一致=未转 GCJ |
| E4 日新立交 | 25.00518,102.73142 | **25.0032,102.7321** | OSM 立交节点转 GCJ |

区划修正：J1/H12/J6 官渡→**西山**（广福路北=西山区前卫街道，工商注册地址佐证）。E8/E10/E14/E23/H26 等盘龙—官渡边界点维持官方通报归属（geojson 边界本身近似）。

未动但存疑：H20 万辉星城、H21 玉龙湾隧道（太平新城一带，坐标为近似，待核）。

## 数据与编辑约定

- **不写用户名**；**未淹地点不标记**。
- 小区/路口坐标用高德目视核对路名，勿单信 Nominatim（WGS 且 POI 覆盖差）。
- 改完跑 `scripts-tmp` 式审计：点是否落在对应行政区 polygon 内（注意边界近似，官方通报归属优先）。
- PEM 勿提交公开仓库（已在 `.gitignore`）。

## 新会话可接着做

- [ ] 继续消化评论区地点补点。
- [ ] 搜索栏体验（可选接入高德 Web Key，国内 POI 更稳）。
- [ ] 经开示意边界 refinement；行政区界精度（盘龙—官渡边界段）。
- [ ] H20/H21 安宁两点坐标核实。

## 关键文件

| 路径 | 作用 |
|------|------|
| `kunming-flood-map/html/index.html` | 页面骨架（线上同） |
| `kunming-flood-map/html/js/data.js` | 点位数据（events + hist，hist 可 ref 事件点） |
| `kunming-flood-map/html/js/app.js` | 应用逻辑（坐标系、视图、搜索、统计） |
| `kunming-flood-map/html/css/app.css` | 全部样式 |
| `kunming-flood-map/html/districts.geojson` | 分区行政/示意边界 |
| `kunming-flood-map/deploy/nginx.conf` | CSP / 缓存 / 安全头 |
| `kunming-flood-map/scripts/publish.ps1` | 打包 + scp + 重启容器 |
| `用户反馈溯源-小红书评论.csv` | 反馈溯源 |

## 快速自检清单

1. 改 `kunming-flood-map/html/` 下文件（数据只改 js/data.js）。
2. 跑 `publish.ps1`，输出含 `done http://43.180.135.43:8088/ status=200`。
3. 浏览器强刷：西山分区看永昌/华昌×采莲是否在云纺旁；搜「永昌小区」「十一家具」；区域分析数字是否刷新。
