# CONTEXT · 昆明积水地图（kunming-flood-map）

单上下文领域文档。协作者先读本文件 + `AGENTS.md`；发布细节在个人 skill `~/.cursor/skills/kunming-flood-deploy`（不在本仓库，由维护者私发）。

## 领域概要

「淹了么 / 昆明积水地图」：把昆明汛期**公开通报** + **小红书用户报点**整理成可点的静态 Web 地图，供绕行避险。

- 静态站：HTML + CSS + JS + `data.js`，无后端
- 底图：高德瓦片（**GCJ-02**）
- 部署：Docker nginx，VPS（地址/凭据见个人 deploy skill，通过环境变量注入）

## 术语表（glossary）

| 词 | 含义 |
|----|------|
| 事件点 `events` | 调研/官方/用户报的积水点（红针），带场次 |
| 常年易淹 `hist` | 历史易淹 / 用户图层（蓝针），可 `ref` 继承事件点坐标 |
| 用户反馈 `feedback` | 不进地图或仅溯源的反馈表 |
| 场次 `evt` | 一次降雨过程：`0716` `0717` `0727` `0802` `0810` `0818` |
| 形态 `morph` | `road` 主干道低洼/交叉口 · `tunnel` 下穿/立交底层 · `culvert` 涵洞/桥洞 · `slow` 缓行/浅积 |
| 梯队榜 | 区域分析里按点数分档的区县榜，**官渡固定「夯」** |
| 热点走廊 | 区域分析里六大高频积水走廊（双排展示） |
| 区县级 `district` | 昆明 14 个区县；**经开→官渡，高新→五华**（在 `app.js` 的 `districtKey` 收敛） |
| GCJ-02 / WGS84 / BD-09 | 高德坐标系 / GPS 坐标系 / 百度坐标系（禁止直接粘贴 BD-09） |

## 行政区划依据（权威标准）

区县划分依据昆明市人民政府门户 **《行政区划》（km.gov.cn/c/2024-02-19/4353631.shtml，来源：市民政局）**：截至 2023 年末，昆明市辖 **7 区（呈贡、五华、盘龙、官渡、西山、晋宁、东川）+ 6 县（嵩明、宜良、富民、石林、禄劝、寻甸）+ 代管安宁市（1 县级市）= 14 个县（市）区**。

经开区、高新区、滇池度假区等为**功能区（开发区）**，不是县级行政区；行政上分别隶属官渡区、五华区。故本图数据模型遵循：

- `district` 字段只写**行政区**（如 五华、官渡），不写功能区名；
- `app.js` 的 `districtKey` 将 经开→官渡、高新→五华、度假→西山 收敛；
- 分区分图 `#v=d-经开` 作为 `#v=d-官渡` 的别名（hash 路由已做重定向）。

## 数据模型（唯一事实源：`kunming-flood-map/html/js/data.js`）

`window.FLOOD_DATA = { events: [...], hist: [...], feedback: [...] }`。改点位只改这里。

### `events`（红针）
| 字段 | 约束 |
|------|------|
| `id` | 唯一，如 `E1` `J1` `U1`（前缀区分场次来源） |
| `evt` | `0716` `0717` `0727` `0802` `0810` `0818` |
| `kind` | `closed` 断交 / `mid` 中度 / `slow` 缓行 / `ctrl` 管制 |
| `morph` | `road` `tunnel` `culvert` `slow` |
| `district` | 区县；写法遵循「经开→官渡、高新→五华」 |
| `lat` `lng` | **GCJ-02** |
| `source` | 原文出处（信息港、交警、小红书等），**必填** |

### `hist`（蓝针）
- 带 `ref: "E1"` 的点**坐标继承**对应事件点，勿两处维护
- `cat`：`tunnel` `road` `river` `new` `ugc` `safe`

### `feedback`（反馈表）
- `status`：`已入库` / `负例不入库` / `存疑未入库`（数据现亦出现 `已有点（细化）`，属扩展值）
- 负例/存疑**不插针**，仅在表中记一笔

## 坐标铁律

| 来源 | 处理 |
|------|------|
| 高德 / 用户高德截图 | 已是 GCJ，直接写 `data.js` |
| OSM / Nominatim / GPS | WGS84 → 用 `app.js` 的 `wgs84ToGcj02(lng, lat)` |
| 百度 | BD-09，**禁止**直接粘贴 |

落点后跑 `node kunming-flood-map/scripts/check-coords.mjs`。

## 页面视图（顶栏，`index.html` + `app.js`）

| 视图 | 作用 |
|------|------|
| 全部淹水 / 各场次 | 按 `evt` 筛事件点 |
| 常年易淹 | `hist` 图层 |
| 区域分析 | 区县卡片 + 梯队榜（官渡「夯」）+ 六大热点走廊双排 + 形态统计 |
| 用户反馈 | 致谢热心市民 + 反馈表（可点进地图） |
| 分区分图 | `#v=d-官渡` 等，浅蓝边界 + 该区点位 |

可分享 URL：`#v=0818` / `#v=d-西山` / `#v=ugc` / `#v=report`。

## 协作约定

- **数据改动顺序**：`kunming-flood-coords`（选点/写库）→ `kunming-flood-verify`（强制核验）→ 个人 `kunming-flood-deploy`（发布）。凡改 `data.js` 坐标，未跑 verify 不得提交/发布。
- **勿提交**：`*.pem`、SSH 私钥、`会话交接*.md`、根目录未整理的 `districts.geojson` 与截图 png。
- 改 `data.js` **务必附来源链接**。
- 新增一场次需同步改：`data.js` → `app.js`（`PERIOD`/`NEWS`/`sourceLinksFor`）→ `index.html` 顶栏按钮。
